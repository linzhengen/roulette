type Member {
  id: ID! @unique
  name: String!
  weight: Int
}

type Item {
  id: ID! @unique
  name: String!
  weight: Int
}

type RouletteMember {
  id: ID! @unique
  member: Member!
  hitAt: DateTime!
}

type RouletteItem {
  id: ID! @unique
  item: Item!
  hitAt: DateTime!
}
