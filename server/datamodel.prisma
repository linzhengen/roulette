type User {
  id: ID! @unique
  name: String!
  englishName: String
  email: String! @unique
  password: String!
  hitWeight: Int
}

type RouletteHistory {
  id: ID! @unique
  user: User!
  hittedAt: DateTime!
}
